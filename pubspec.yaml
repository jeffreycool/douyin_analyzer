name: douyin_analyzer
description: "Douyin video content analyzer - extract audio, ASR transcription, AI summary"
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.6.1

dependencies:
  flutter:
    sdk: flutter
  get: ^4.6.6
  http: ^1.2.2
  path_provider: ^2.1.5
  path: ^1.9.0
  flutter_markdown: ^0.7.7
  share_plus: ^10.1.4
  url_launcher: ^6.3.1
  cupertino_icons: ^1.0.8

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
